[sql]
sql_pro = SELECT DISTINCT(p.PROVINCE),p.provinceid FROM shop_info s INNER JOIN area a ON s.AREA_CODE = a.AREAID AND s.shop_status = 3 INNER JOIN city c ON c.CITYID = a.FATHERID INNER JOIN province p ON p.PROVINCEID = c.FATHERID
sql_city = SELECT DISTINCT(c.city), c.cityid FROM shop_info s INNER JOIN area a ON s.AREA_CODE = a.AREAID AND s.shop_status = 3 INNER JOIN city c ON c.CITYID = a.FATHERID INNER JOIN province p ON p.PROVINCEID = c.FATHERID where p.provinceid=
sql_area = SELECT DISTINCT(a.area), a.areaid FROM shop_info s INNER JOIN area a ON s.AREA_CODE = a.AREAID AND s.shop_status = 3 INNER JOIN city c ON c.CITYID = a.FATHERID INNER JOIN province p ON p.PROVINCEID = c.FATHERID WHERE c.cityid=
sql_payrate1 = select round(sum(real_amount),2) as  "实际支付",round(sum(bill_time_precharge),2) as "E币",round(sum(discount),2) as "优惠券"from shop_dailyorder_income where province="province_" and city="city_" and area="area_"
sql_payrate2 = select round(sum(real_amount),2) as  "实际支付",round(sum(bill_time_precharge),2) as "E币",round(sum(discount),2) as "优惠券"from shop_dailyorder_income where province="province_" and city="city_"
sql_payrate3 = select round(sum(real_amount),2) as  "实际支付",round(sum(bill_time_precharge),2) as "E币",round(sum(discount),2) as "优惠券"from shop_dailyorder_income where province="province_"
sql_payrate4 = select round(sum(real_amount),2) as  "实际支付",round(sum(bill_time_precharge),2) as "E币",round(sum(discount),2) as "优惠券" from shop_dailyorder_income
